{"config":{"separator":"[\\s\\-_,:!=\\[\\]()\\\\\"`/]+|\\.(?!\\d)"},"items":[{"location":"index.html","level":1,"title":"Talika","text":"<p>   Talika - Hindi for Tables  Declarative schemas for your test data tables. </p> <p> </p> <p> Define your table shape once with Python types and descriptors.   Talika handles parsing, cell conversion, validation, and precise error reporting — and lets your team define its own readable cell conventions instead of fighting <code>list[list[str]]</code> </p> <p>The key features are:</p> <ul> <li>Short: Eliminate manual parsing boilerplate and stop slicing raw <code>list[list[str]]</code> inputs.</li> <li>Typed: Declare your schema using standard Python types for automatic cell conversion.</li> <li>Fewer bugs: Validate required fields and exact types before your test steps even execute.</li> <li>Pinpoint errors: Get exact line and column locations in your <code>.feature</code> file when data fails validation.</li> <li>Expressive: Define custom <code>cell conventions</code>, fallbacks, and regex matchers with the <code>built-in DSL</code>.</li> <li>Polymorphic: Parse rows representing completely different object types using <code>clean variant schemas</code>.</li> <li>Flexible: Support both Row-oriented (lists) and Column-oriented (single items) tables seamlessly.</li> <li>Static validation: Use the <code>talika check</code> CLI to validate Gherkin tables in CI without booting your test suite.</li> </ul> Built By<pre><code>$ Chinmay\nGithub - https://github.com/chinmaysingh31\nEmail - chinmay@gmail.com\n$ Nishant\nGithub - https://github.com/github\nEmail - nishant@gmail.com\n</code></pre>","path":["Talika"],"tags":[]},{"location":"guides/advanced/cell-dsl-composition.html","level":1,"title":"CellDSL Composition","text":"<p>Use composition when you have shared table vocabulary plus feature-specific rules.</p> <pre><code>from talika import CellDSL, compose_cell_dsls\n\nshared = CellDSL()\ncontent = CellDSL()\n\n\n@shared.token(\"none\")\ndef none_value(context):\n    return None\n\n\n@content.token(\"random\", fields=(\"headline\",))\ndef random_headline(context):\n    return context.user_data[\"faker\"].headline()\n\n\nparser = compose_cell_dsls(content, shared)\n</code></pre> <p>The first DSL that matches owns the value. If none match, the original text passes through.</p>","path":["Guides","Advanced","CellDSL Composition"],"tags":[]},{"location":"guides/advanced/cell-dsl-composition.html#precedence","level":2,"title":"Precedence","text":"<p>Dispatch order inside one DSL is:</p> <ol> <li>exact tokens</li> <li>patterns</li> <li>predicates</li> <li>fallback</li> </ol> <p>Composition adds one more layer: DSLs are consulted left-to-right.</p>","path":["Guides","Advanced","CellDSL Composition"],"tags":[]},{"location":"guides/advanced/cell-dsl-patterns.html","level":1,"title":"CellDSL Patterns","text":"<p>Patterns use regular-expression <code>fullmatch</code>.</p> <pre><code>from talika import CellDSL\n\ncells = CellDSL()\n\n\n@cells.pattern(r\"(?P&lt;count&gt;\\d+) words\")\ndef generated_words(match, context):\n    return context.user_data[\"faker\"].words(int(match[\"count\"]))\n</code></pre> <p>The whole cell must match the pattern. Use <code>.*</code> explicitly if your project really wants substring behavior.</p>","path":["Guides","Advanced","CellDSL Patterns"],"tags":[]},{"location":"guides/advanced/cell-dsl-patterns.html#predicates","level":2,"title":"Predicates","text":"<p>Use predicates when a rule is awkward to express as regex.</p> <pre><code>@cells.when(lambda value, context: value.startswith(\"QA-\"))\ndef qa_value(value, context):\n    return value.removeprefix(\"QA-\")\n</code></pre> <p>Predicates run after exact tokens and patterns.</p>","path":["Guides","Advanced","CellDSL Patterns"],"tags":[]},{"location":"guides/advanced/cell-dsl-patterns.html#fallback","level":2,"title":"Fallback","text":"<p>A fallback handles values that match no explicit rule.</p> <pre><code>@cells.fallback\ndef fallback(value, context):\n    return value.upper()\n</code></pre> <p>A DSL with a fallback matches every value, which matters when composing DSLs.</p>","path":["Guides","Advanced","CellDSL Patterns"],"tags":[]},{"location":"guides/advanced/cell-dsl-tokens.html","level":1,"title":"CellDSL Tokens","text":"<p><code>CellDSL</code> lets a project promote table vocabulary into reusable parser rules.</p> <pre><code>from talika import CellDSL\n\ncells = CellDSL()\n\n\n@cells.token(\"random\", fields=(\"headline\",))\ndef random_headline(context):\n    return context.user_data[\"faker\"].headline()\n</code></pre> <p>Attach the DSL as a field parser:</p> <pre><code>class ContentTable(ColumnTable):\n    id = id_field(\"IDs\")\n    headline = field(\"Headline\", parser=cells)\n</code></pre> <p>Now <code>random</code> has a documented meaning for <code>headline</code>.</p>","path":["Guides","Advanced","CellDSL Tokens"],"tags":[]},{"location":"guides/advanced/cell-dsl-tokens.html#field-scoping","level":2,"title":"Field scoping","text":"<p>Scopes use Python field names, not table labels.</p> <pre><code>headline = field(\"Headline\")\n</code></pre> <p>The scope is <code>\"headline\"</code>.</p> <p>Field-scoped tokens take precedence over global tokens with the same value.</p>","path":["Guides","Advanced","CellDSL Tokens"],"tags":[]},{"location":"guides/advanced/collect-errors.html","level":1,"title":"Collect Errors","text":"<p>By default, Talika stops at the first table error.</p> <pre><code>UserTable.parse(datatable)\n</code></pre> <p>Use collect mode when authors should fix several independent problems in one pass.</p> <pre><code>UserTable.parse(datatable, error_mode=\"collect\")\n</code></pre>","path":["Guides","Advanced","Collect Errors"],"tags":[]},{"location":"guides/advanced/collect-errors.html#tableerrors","level":2,"title":"TableErrors","text":"<p>Collect mode raises <code>TableErrors</code>, an aggregate of <code>TableError</code> objects.</p> <pre><code>Table contains 2 errors:\n  1. Required field has an empty value (code=empty_required, schema=UserTable, field='name', row=2, column=1, value=''). Hint: Fill the cell, or remove required=True if an explicit empty value should be valid.\n  2. Field parser failed: invalid literal for int() with base 10: 'old' (code=parser_failed, schema=UserTable, field='age', row=2, column=2, value='old'). Hint: Check the cell value or adjust the field parser for this syntax.\n</code></pre>","path":["Guides","Advanced","Collect Errors"],"tags":[]},{"location":"guides/advanced/collect-errors.html#tooling-value","level":2,"title":"Tooling value","text":"<p>Every contained error keeps its stable <code>code</code>, source coordinates, field, value, and hint. That makes collect mode useful for CI, editor diagnostics, and feature file linting.</p>","path":["Guides","Advanced","Collect Errors"],"tags":[]},{"location":"guides/advanced/column-group-expansion.html","level":1,"title":"Column Group Expansion","text":"<p><code>ColumnGroupExpander</code> handles compact column-oriented tables where one source column expands into several logical item columns.</p> <pre><code>from talika import ColumnGroupExpander, NumericRange, PrefixRepeat\n\nexpander = ColumnGroupExpander(\n    key_row=\"IDs\",\n    range_rule=NumericRange(\"..\"),\n    repeat_rule=PrefixRepeat(\":\"),\n)\n</code></pre> <pre><code>from talika import ColumnTable, field, id_field\n\n\nclass ContentTable(ColumnTable):\n    table_transformer = expander\n\n    id = id_field(\"IDs\")\n    content_type = field(\"Type\")\n</code></pre> <p>This source table:</p> <pre><code>[\n    [\"IDs\", \"1..3\"],\n    [\"Type\", \"3:Article\"],\n]\n</code></pre> <p>becomes:</p> <pre><code>[\n    [\"IDs\", \"1\", \"2\", \"3\"],\n    [\"Type\", \"Article\", \"Article\", \"Article\"],\n]\n</code></pre>","path":["Guides","Advanced","Column Group Expansion"],"tags":[]},{"location":"guides/advanced/column-group-expansion.html#built-in-rules","level":2,"title":"Built-in rules","text":"<ul> <li><code>NumericRange(\"..\")</code>: <code>1..3</code></li> <li><code>AlphabeticRange(\"-\")</code>: <code>A-C</code></li> <li><code>PrefixRepeat(\":\")</code>: <code>3:Article</code></li> <li><code>SuffixRepeat(\" x\")</code>: <code>Article x3</code></li> </ul> <p>Custom rules can implement any project convention, but must return <code>TableCell</code> objects so source metadata is preserved.</p>","path":["Guides","Advanced","Column Group Expansion"],"tags":[]},{"location":"guides/advanced/inapplicable-fields.html","level":1,"title":"Inapplicable Fields","text":"<p>Variant tables often include the union of all possible fields.</p> <pre><code>| type    | body         | options |\n| Article | Article body |         |\n| Poll    |              | Yes, No |\n</code></pre> <p>An empty cell for another variant is ignored. A non-empty cell for another variant is rejected by default.</p>","path":["Guides","Advanced","Inapplicable Fields"],"tags":[]},{"location":"guides/advanced/inapplicable-fields.html#forbid","level":2,"title":"Forbid","text":"<p>The default policy is:</p> <pre><code>inapplicable_fields = \"forbid\"\n</code></pre> <p>This catches a value written into a field that does not belong to the selected variant.</p>","path":["Guides","Advanced","Inapplicable Fields"],"tags":[]},{"location":"guides/advanced/inapplicable-fields.html#preserve","level":2,"title":"Preserve","text":"<p>Use <code>\"preserve\"</code> when legacy tables may contain old values you still want to inspect.</p> <pre><code>class ContentTable(RowTable):\n    inapplicable_fields = \"preserve\"\n</code></pre> <p>Preserved values are available as <code>record.table_extras</code>.</p> <pre><code>assert poll.table_extras == {\"body\": \"legacy\"}\n</code></pre> <p><code>\"ignore\"</code> is not supported today.</p>","path":["Guides","Advanced","Inapplicable Fields"],"tags":[]},{"location":"guides/advanced/output-models.html","level":1,"title":"Output Models","text":"<p>By default, Talika returns schema record objects. Set <code>output_model</code> when you want your own types.</p> <pre><code>from dataclasses import dataclass\nfrom talika import RowTable, field\n\n\n@dataclass(frozen=True)\nclass User:\n    name: str\n    age: int\n\n\nclass UserTable(RowTable):\n    output_model = User\n\n    name = field(\"name\")\n    age: int = field(\"age\")\n</code></pre> <pre><code>users = UserTable.parse([\n    [\"name\", \"age\"],\n    [\"Alice\", \"30\"],\n])\n\nassert users == [User(name=\"Alice\", age=30)]\n</code></pre>","path":["Guides","Advanced","Output Models"],"tags":[]},{"location":"guides/advanced/output-models.html#parse_records","level":2,"title":"parse_records","text":"<p><code>parse_records()</code> skips output conversion:</p> <pre><code>records = UserTable.parse_records(datatable)\n</code></pre> <p>Use it when you need source metadata or schema record methods.</p>","path":["Guides","Advanced","Output Models"],"tags":[]},{"location":"guides/advanced/output-models.html#custom-output-builder","level":2,"title":"Custom output builder","text":"<p>Override <code>build_output()</code> for custom factories:</p> <pre><code>class UserTable(RowTable):\n    name = field(\"name\")\n\n    @classmethod\n    def build_output(cls, record, context):\n        return {\"user\": record.name}\n</code></pre> <p>Pydantic v2 models work through the same <code>output_model</code> contract. Install <code>talika[pydantic]</code> only when you use that integration.</p>","path":["Guides","Advanced","Output Models"],"tags":[]},{"location":"guides/advanced/references.html","level":1,"title":"References","text":"<p>Use <code>reference()</code> for local links between records in the same table.</p> <pre><code>from talika import ColumnTable, field, id_field, reference\n\n\nclass ContentTable(ColumnTable):\n    id = id_field(\"IDs\")\n    headline = field(\"Headline\")\n    parent = reference(\"Parent\", target=\"id\")\n    related = reference(\"Related\", target=\"id\", many=True)\n</code></pre> <pre><code>items = ContentTable.parse([\n    [\"IDs\", \"1\", \"2\", \"3\"],\n    [\"Headline\", \"Root\", \"Child\", \"Other\"],\n    [\"Parent\", \"\", \"1\", \"1\"],\n    [\"Related\", \"2, 3\", \"\", \"2\"],\n])\n</code></pre> <pre><code>assert items[1].parent is items[0]\nassert items[0].related == [items[1], items[2]]\n</code></pre>","path":["Guides","Advanced","References"],"tags":[]},{"location":"guides/advanced/references.html#local-by-design","level":2,"title":"Local by design","text":"<p>References resolve only within the table being parsed. There is no global registry, no scenario-to-scenario state, and no external lookup.</p>","path":["Guides","Advanced","References"],"tags":[]},{"location":"guides/advanced/references.html#typed-ids","level":2,"title":"Typed IDs","text":"<p>Reference keys are parsed with the target field parser before lookup.</p> <pre><code>from talika import integer\n\n\nclass ContentTable(ColumnTable):\n    id = id_field(\"IDs\", parser=integer())\n    parent = reference(\"Parent\")\n</code></pre> <p>The raw cell <code>\"1\"</code> resolves to the record whose parsed <code>id</code> is <code>1</code>.</p>","path":["Guides","Advanced","References"],"tags":[]},{"location":"guides/advanced/schema-contracts.html","level":1,"title":"Schema Contracts","text":"<p>Use <code>describe()</code> to inspect a schema without parsing a table.</p> <pre><code>contract = UserTable.describe()\n\nassert contract.orientation == \"row\"\n</code></pre> <p>The returned <code>TableContract</code> is frozen and machine-readable.</p> <pre><code>payload = contract.as_dict()\n</code></pre> <p>Contracts include:</p> <ul> <li>schema name</li> <li>orientation</li> <li>fields</li> <li>variants</li> <li>policies</li> <li>transformer name</li> <li>output model name</li> <li>output builder name</li> </ul>","path":["Guides","Advanced","Schema Contracts"],"tags":[]},{"location":"guides/advanced/schema-contracts.html#cli-describe","level":2,"title":"CLI describe","text":"<p>The CLI exposes the same contract:</p> <pre><code>talika describe app.schemas:UserTable\ntalika describe app.schemas:UserTable --format json\n</code></pre> <p>This is useful for generated docs, editor integrations, and schema drift checks in CI.</p>","path":["Guides","Advanced","Schema Contracts"],"tags":[]},{"location":"guides/advanced/source-model.html","level":1,"title":"Source Model","text":"<p>Talika accepts raw rows, then upgrades them to source-aware objects.</p> <pre><code>from talika import TableData\n\ntable = TableData.from_rows([\n    [\"name\"],\n    [\"Alice\"],\n])\n</code></pre> <p>Every cell has current value and original source information.</p> <pre><code>cell = table.cell(2, 1)\nassert cell.value == \"Alice\"\nassert cell.source_row == 2\nassert cell.source_column == 1\nassert cell.source_value == \"Alice\"\n</code></pre>","path":["Guides","Advanced","Source Model"],"tags":[]},{"location":"guides/advanced/source-model.html#transforming-values","level":2,"title":"Transforming values","text":"<p>Use <code>with_value()</code> when a transformer changes a value.</p> <pre><code>changed = cell.with_value(\"ALICE\")\nassert changed.value == \"ALICE\"\nassert changed.source_value == \"Alice\"\n</code></pre> <p>That is how later errors can point to the feature text that caused the logical value.</p>","path":["Guides","Advanced","Source Model"],"tags":[]},{"location":"guides/advanced/source-model.html#records","level":2,"title":"Records","text":"<p>Parsed records expose source metadata:</p> <pre><code>source_cell = record.source_for(\"name\")\nitem_id = record.table_source.item_id\n</code></pre>","path":["Guides","Advanced","Source Model"],"tags":[]},{"location":"guides/advanced/static-checking.html","level":1,"title":"Static Checking","text":"<p>Install the CLI extra:</p> Install CLI Extra<pre><code>$ pip install \"talika[cli]\"\n</code></pre> <p>Then validate feature-file tables without running pytest scenarios:</p> Validate Feature Tables<pre><code>$ talika check features/users.feature \\\n  --schema app.schemas:UserTable \\\n  --step \"the users:\" \\\n  --format json\n</code></pre>","path":["Guides","Advanced","Static Checking"],"tags":[]},{"location":"guides/advanced/static-checking.html#what-check-runs","level":2,"title":"What check runs","text":"<p><code>talika check</code> discovers matching Gherkin data tables, converts them to source-aware <code>TableData</code>, and parses them with <code>error_mode=\"collect\"</code>.</p> <p>That means normal parsers and validators run. If they need deterministic project dependencies, provide a context factory:</p> Check With Context<pre><code>$ talika check features/users.feature \\\n  --schema app.schemas:UserTable \\\n  --context-factory app.schemas:checking_context\n</code></pre> <p>The factory must be a zero-argument callable that returns a mapping.</p>","path":["Guides","Advanced","Static Checking"],"tags":[]},{"location":"guides/advanced/static-checking.html#exit-codes","level":2,"title":"Exit codes","text":"<ul> <li><code>0</code>: valid</li> <li><code>1</code>: validation failures</li> <li><code>2</code>: filters matched no tables</li> </ul> <p>Use <code>--scenario</code> and <code>--step</code> to narrow which tables are checked.</p>","path":["Guides","Advanced","Static Checking"],"tags":[]},{"location":"guides/advanced/transform-tables.html","level":1,"title":"Transform Tables","text":"<p>Use table transformations when the authored table shape is not the shape your schema should parse directly.</p> <pre><code>from talika import RowTable, TableData, field\n\n\nclass UpperTable(RowTable):\n    value = field(\"value\")\n\n    @classmethod\n    def transform_table(cls, table, context):\n        rows = []\n        for row in table.rows:\n            rows.append([cell.with_value(cell.value.upper()) for cell in row])\n        return TableData.from_cells(rows)\n</code></pre> <p><code>transform_table()</code> receives <code>TableData</code> and must return <code>TableData</code>.</p>","path":["Guides","Advanced","Transform Tables"],"tags":[]},{"location":"guides/advanced/transform-tables.html#reusable-transformers","level":2,"title":"Reusable transformers","text":"<p>Reusable transformer objects implement:</p> <pre><code>def transform(self, table, context, *, schema=None):\n    ...\n</code></pre> <p>Attach them with <code>table_transformer</code>:</p> <pre><code>class MyTable(RowTable):\n    table_transformer = my_transformer\n</code></pre> <p>Use <code>TransformerPipeline</code> or <code>compose_transformers()</code> to run several transformers left-to-right.</p>","path":["Guides","Advanced","Transform Tables"],"tags":[]},{"location":"guides/advanced/transform-tables.html#source-preservation","level":2,"title":"Source preservation","text":"<p>When a transformed value fails later, Talika reports the original source cell if you used <code>with_value()</code>.</p>","path":["Guides","Advanced","Transform Tables"],"tags":[]},{"location":"guides/advanced/variants.html","level":1,"title":"Variants","text":"<p>Use variants when one table contains records with different shapes.</p> <pre><code>from talika import ColumnTable, TableFields, discriminator, field, id_field, split\n\n\nclass ArticleFields(TableFields):\n    body = field(\"Body\", required=True)\n\n\nclass PollFields(TableFields):\n    options = field(\"Options\", required=True, parser=split(\",\"))\n\n\nclass ContentTable(ColumnTable):\n    id = id_field(\"IDs\")\n    content_type = discriminator(\n        \"Type\",\n        variants={\"Article\": ArticleFields, \"Poll\": PollFields},\n    )\n    headline = field(\"Headline\", required=True)\n</code></pre> <pre><code>items = ContentTable.parse([\n    [\"IDs\", \"1\", \"2\"],\n    [\"Type\", \"Article\", \"Poll\"],\n    [\"Headline\", \"News\", \"Choose\"],\n    [\"Body\", \"Article body\", \"\"],\n    [\"Options\", \"\", \"Yes, No\"],\n])\n</code></pre> <p>Each record is an instance of the base table and the selected <code>TableFields</code> component.</p>","path":["Guides","Advanced","Variants"],"tags":[]},{"location":"guides/advanced/variants.html#explicit-variant-classes","level":2,"title":"Explicit variant classes","text":"<p>Use <code>discriminator_field()</code> plus decorators when you want named classes:</p> <pre><code>class PaymentTable(RowTable):\n    payment_type = discriminator_field(\"type\")\n    amount: int = field(\"amount\", required=True)\n\n\n@PaymentTable.variant(\"card\")\nclass CardPayment(PaymentTable):\n    last_four = field(\"last_four\", required=True)\n</code></pre> <p>The discriminator parser runs before variant lookup, so register parsed values.</p>","path":["Guides","Advanced","Variants"],"tags":[]},{"location":"guides/basic/column-tables.html","level":1,"title":"Column Tables","text":"<p>Use <code>ColumnTable</code> when the first column contains labels and each later column is one item.</p> <pre><code>from talika import ColumnTable, field, id_field\n\n\nclass ContentTable(ColumnTable):\n    id = id_field(\"IDs\")\n    content_type = field(\"Type\", required=True)\n    headline = field(\"Headline\", required=True)\n    category = field(\"Category\")\n</code></pre> <pre><code>items = ContentTable.parse([\n    [\"IDs\", \"1\", \"2\"],\n    [\"Type\", \"Article\", \"Poll\"],\n    [\"Headline\", \"Hello\", \"Choose one?\"],\n    [\"Category\", \"Markets\", \"\"],\n])\n</code></pre> <p>The first row must be the declared <code>id_field</code>. Item IDs are available in parsed records and diagnostics.</p> <pre><code>assert [item.id for item in items] == [\"1\", \"2\"]\n</code></pre>","path":["Guides","Basic","Column Tables"],"tags":[]},{"location":"guides/basic/column-tables.html#missing-optional-rows","level":2,"title":"Missing optional rows","text":"<p>If an optional row is absent, its value is <code>None</code> unless you declared a default.</p> <pre><code>items = ContentTable.parse([\n    [\"IDs\", \"1\"],\n    [\"Type\", \"Article\"],\n    [\"Headline\", \"Hello\"],\n])\n\nassert items[0].category is None\n</code></pre>","path":["Guides","Basic","Column Tables"],"tags":[]},{"location":"guides/basic/column-tables.html#duplicate-ids","level":2,"title":"Duplicate IDs","text":"<p>Every item column needs a unique ID. Duplicate IDs raise <code>duplicate_id</code> with the source cell location.</p>","path":["Guides","Basic","Column Tables"],"tags":[]},{"location":"guides/basic/custom-parsers.html","level":1,"title":"Custom Parsers","text":"<p>A parser receives the current cell value and a <code>CellContext</code>.</p> <pre><code>from talika import RowTable, field\n\n\ndef parse_code(value, context):\n    prefix = context.user_data[\"prefix\"]\n    return f\"{prefix}{value}\"\n\n\nclass CodeTable(RowTable):\n    code = field(\"code\", parser=parse_code)\n</code></pre> <pre><code>record = CodeTable.parse(\n    [[\"code\"], [\"A1\"]],\n    context={\"prefix\": \"user-\"},\n)[0]\n\nassert record.code == \"user-A1\"\n</code></pre>","path":["Guides","Basic","Custom Parsers"],"tags":[]},{"location":"guides/basic/custom-parsers.html#cellcontext","level":2,"title":"CellContext","text":"<p>The context includes:</p> <ul> <li><code>schema</code></li> <li><code>field_name</code></li> <li><code>field_label</code></li> <li><code>row</code></li> <li><code>column</code></li> <li><code>item_id</code></li> <li><code>source_value</code></li> <li><code>user_data</code></li> </ul> <p><code>value</code> may already be transformed by a table transformer. <code>source_value</code> records what was written in the original feature table.</p>","path":["Guides","Basic","Custom Parsers"],"tags":[]},{"location":"guides/basic/custom-parsers.html#parser-failures","level":2,"title":"Parser failures","text":"<p>If a custom parser raises an ordinary exception, Talika wraps it:</p> <pre><code>Field parser failed: not accepted (code=parser_failed, schema=CodeTable, field='code', row=2, column=1, value='bad')\n</code></pre> <p>Raise <code>TableError</code> yourself when you need full control over the diagnostic.</p>","path":["Guides","Basic","Custom Parsers"],"tags":[]},{"location":"guides/basic/fields.html","level":1,"title":"Fields","text":"<p>Fields map human-facing table labels to Python attributes.</p> <pre><code>from talika import RowTable, field\n\n\nclass UserTable(RowTable):\n    name = field(\"Full name\", required=True)\n</code></pre> <p>The feature table uses <code>Full name</code>. Python code reads <code>user.name</code>.</p>","path":["Guides","Basic","Fields"],"tags":[]},{"location":"guides/basic/fields.html#required-fields","level":2,"title":"Required fields","text":"<pre><code>name = field(\"name\", required=True)\n</code></pre> <p>Required fields must be present and non-empty.</p>","path":["Guides","Basic","Fields"],"tags":[]},{"location":"guides/basic/fields.html#aliases","level":2,"title":"Aliases","text":"<p>Aliases let feature vocabulary evolve without breaking old tables.</p> <pre><code>name = field(\"name\", aliases=(\"full name\", \"display name\"), required=True)\n</code></pre> <p>The canonical label and one of its aliases cannot appear in the same table. Talika reports that as <code>duplicate_label</code>.</p>","path":["Guides","Basic","Fields"],"tags":[]},{"location":"guides/basic/fields.html#unknown-fields","level":2,"title":"Unknown fields","text":"<p>Talika rejects undeclared labels by default and today supports only:</p> <pre><code>unknown_fields = \"forbid\"\n</code></pre> <p>That is intentional for now: unexpected table columns should be visible because they often mean a typo, stale feature file, or schema drift.</p>","path":["Guides","Basic","Fields"],"tags":[]},{"location":"guides/basic/fields.html#labels-are-literal","level":2,"title":"Labels are literal","text":"<p>Characters such as <code>*</code> have no built-in meaning.</p> <pre><code>headline = field(\"Headline*\", required=True)\n</code></pre> <p>The <code>*</code> is just part of the label. <code>required=True</code> is what makes the field required.</p>","path":["Guides","Basic","Fields"],"tags":[]},{"location":"guides/basic/missing-empty-defaults.html","level":1,"title":"Missing, Empty, Defaults","text":"<p>Talika treats a missing field and an explicit empty cell differently.</p>","path":["Guides","Basic","Missing, Empty, Defaults"],"tags":[]},{"location":"guides/basic/missing-empty-defaults.html#missing-field","level":2,"title":"Missing field","text":"<p>A field is missing when its label is not present in the table.</p> <pre><code>class UserTable(RowTable):\n    active = field(\"active\", default=True)\n</code></pre> <pre><code>user = UserTable.parse([[\"name\"], [\"Alice\"]])[0]\nassert user.active is True\n</code></pre>","path":["Guides","Basic","Missing, Empty, Defaults"],"tags":[]},{"location":"guides/basic/missing-empty-defaults.html#default-factory","level":2,"title":"Default factory","text":"<p>Factories run only when an optional field is absent.</p> <pre><code>def generated(context):\n    return f\"generated-{context.item_id}\"\n\n\nclass ContentTable(ColumnTable):\n    id = id_field(\"IDs\")\n    headline = field(\"Headline\", default_factory=generated)\n</code></pre> <p>Factories receive <code>DefaultContext</code>, including the schema, field, item ID, and read-only parse context data.</p>","path":["Guides","Basic","Missing, Empty, Defaults"],"tags":[]},{"location":"guides/basic/missing-empty-defaults.html#explicit-empty-cells","level":2,"title":"Explicit empty cells","text":"<p>For optional fields, choose an empty-cell policy:</p> <pre><code>field(\"value\", empty=\"raw\")    # keep \"\"\nfield(\"value\", empty=\"parse\")  # send \"\" to the parser\nfield(\"value\", empty=\"none\")   # return None\nfield(\"value\", empty=\"error\")  # reject \"\"\n</code></pre> <p>Required fields always reject explicit empty cells.</p>","path":["Guides","Basic","Missing, Empty, Defaults"],"tags":[]},{"location":"guides/basic/parser-factories.html","level":1,"title":"Parser Factories","text":"<p>Parser factories return callables for <code>field(parser=...)</code>.</p> <pre><code>from talika import RowTable, compose, each, field, integer, split, string\n\n\nclass ScoreTable(RowTable):\n    scores = field(\n        \"scores\",\n        parser=compose(split(\",\"), each(compose(string(strip=True), integer()))),\n    )\n</code></pre> <pre><code>record = ScoreTable.parse([[\"scores\"], [\"1, 2, 3\"]])[0]\nassert record.scores == [1, 2, 3]\n</code></pre>","path":["Guides","Basic","Parser Factories"],"tags":[]},{"location":"guides/basic/parser-factories.html#scalar-parsers","level":2,"title":"Scalar parsers","text":"<ul> <li><code>string(strip=True, lower=True, upper=True)</code></li> <li><code>integer(base=10)</code></li> <li><code>floating()</code></li> <li><code>decimal()</code></li> <li><code>boolean()</code></li> </ul>","path":["Guides","Basic","Parser Factories"],"tags":[]},{"location":"guides/basic/parser-factories.html#vocabulary-parsers","level":2,"title":"Vocabulary parsers","text":"<ul> <li><code>choice(\"Draft\", \"Published\", case_sensitive=False)</code></li> <li><code>map_value({\"high\": 3, \"low\": 1})</code></li> </ul> <p><code>choice()</code> returns the canonical string you configured. <code>map_value()</code> returns the Python value from the mapping.</p>","path":["Guides","Basic","Parser Factories"],"tags":[]},{"location":"guides/basic/parser-factories.html#list-and-pipeline-parsers","level":2,"title":"List and pipeline parsers","text":"<ul> <li><code>split(\",\")</code> converts one cell into <code>list[str]</code></li> <li><code>compose(a, b, c)</code> runs parsers left-to-right</li> <li><code>each(parser)</code> applies a parser to every item in a non-string iterable</li> <li><code>optional(parser)</code> maps empty or null-like values to <code>None</code></li> </ul> <p>Parser failures are wrapped as <code>TableError</code> with the original cell location.</p>","path":["Guides","Basic","Parser Factories"],"tags":[]},{"location":"guides/basic/pytest-bdd.html","level":1,"title":"pytest-bdd","text":"<p>Talika does not replace pytest-bdd. It parses the datatable that pytest-bdd already passes to a step function.</p>","path":["Guides","Basic","pytest-bdd"],"tags":[]},{"location":"guides/basic/pytest-bdd.html#direct-schema-parsing","level":2,"title":"Direct schema parsing","text":"<pre><code>from pytest_bdd import given\n\n\n@given(\"the users:\", target_fixture=\"users\")\ndef users(datatable):\n    return UserTable.parse(datatable)\n</code></pre>","path":["Guides","Basic","pytest-bdd"],"tags":[]},{"location":"guides/basic/pytest-bdd.html#fixture-facade","level":2,"title":"Fixture facade","text":"<p>Talika also registers a <code>talika</code> pytest fixture.</p> <pre><code>@given(\"the users:\", target_fixture=\"users\")\ndef users(datatable, talika):\n    return talika.parse(datatable, schema=UserTable)\n</code></pre> <p>Use <code>parse_records()</code> when the step needs schema records even if the schema has an <code>output_model</code>.</p> <pre><code>records = talika.parse_records(datatable, schema=UserTable)\n</code></pre>","path":["Guides","Basic","pytest-bdd"],"tags":[]},{"location":"guides/basic/pytest-bdd.html#functional-api","level":2,"title":"Functional API","text":"<p>Some teams prefer a function call with the schema as an argument:</p> <pre><code>from talika import parse_table, parse_table_records\n\nusers = parse_table(UserTable, datatable)\nrecords = parse_table_records(UserTable, datatable)\n</code></pre> <p>These helpers delegate to the schema methods. There is only one parsing lifecycle.</p>","path":["Guides","Basic","pytest-bdd"],"tags":[]},{"location":"guides/basic/record-validation.html","level":1,"title":"Record Validation","text":"<p>Use <code>validate_record()</code> for checks that involve one parsed record.</p> <pre><code>from talika import RowTable, field\n\n\nclass UserTable(RowTable):\n    name = field(\"name\")\n    role = field(\"role\")\n\n    def validate_record(self, context):\n        if self.role not in {\"admin\", \"editor\"}:\n            raise ValueError(f\"Unsupported role: {self.role}\")\n</code></pre> <p>Validation runs after parsers and defaults.</p>","path":["Guides","Basic","Record Validation"],"tags":[]},{"location":"guides/basic/record-validation.html#context-aware-validation","level":2,"title":"Context-aware validation","text":"<pre><code>class UserTable(RowTable):\n    role = field(\"role\")\n\n    def validate_record(self, context):\n        if self.role not in context.user_data[\"allowed_roles\"]:\n            raise ValueError(f\"Role {self.role!r} is not allowed\")\n</code></pre> <pre><code>UserTable.parse(\n    [[\"role\"], [\"editor\"]],\n    context={\"allowed_roles\": {\"admin\", \"editor\"}},\n)\n</code></pre>","path":["Guides","Basic","Record Validation"],"tags":[]},{"location":"guides/basic/record-validation.html#source-aware-custom-errors","level":2,"title":"Source-aware custom errors","text":"<p>Use <code>TableError.from_cell()</code> when the error should point at one field cell.</p> <pre><code>from talika import TableError\n\n\nraise TableError.from_cell(\n    \"Invalid role\",\n    self.source_for(\"role\"),\n    schema=type(self),\n)\n</code></pre> <p>Ordinary exceptions are wrapped as <code>record_validation_failed</code>.</p>","path":["Guides","Basic","Record Validation"],"tags":[]},{"location":"guides/basic/row-tables.html","level":1,"title":"Row Tables","text":"<p>Use <code>RowTable</code> when the first row contains labels and each later row is one record.</p> <pre><code>from talika import RowTable, field\n\n\nclass UserTable(RowTable):\n    name = field(\"name\", required=True)\n    role = field(\"role\", required=True)\n    active = field(\"active\", default=True)\n</code></pre> <pre><code>users = UserTable.parse([\n    [\"name\", \"role\", \"active\"],\n    [\"Alice\", \"admin\", \"yes\"],\n    [\"Bob\", \"editor\", \"\"],\n])\n</code></pre> <p><code>users[0]</code> is a <code>UserTable</code> record. Declared fields become attributes:</p> <pre><code>assert users[0].name == \"Alice\"\nassert users[0].role == \"admin\"\n</code></pre>","path":["Guides","Basic","Row Tables"],"tags":[]},{"location":"guides/basic/row-tables.html#missing-optional-fields","level":2,"title":"Missing optional fields","text":"<p>If an optional field is absent from the header row, Talika uses its default or <code>None</code>.</p> <pre><code>users = UserTable.parse([\n    [\"name\", \"role\"],\n    [\"Alice\", \"admin\"],\n])\n\nassert users[0].active is True\n</code></pre>","path":["Guides","Basic","Row Tables"],"tags":[]},{"location":"guides/basic/row-tables.html#required-fields","level":2,"title":"Required fields","text":"<p>A required field must be present and non-empty.</p> <pre><code>Required field is missing (code=missing_required, schema=UserTable, field='role')\n</code></pre> <p>For empty cells, the error includes the row and column.</p>","path":["Guides","Basic","Row Tables"],"tags":[]},{"location":"guides/basic/table-validation.html","level":1,"title":"Table Validation","text":"<p>Use <code>validate_records()</code> for checks that need the whole parsed table.</p> <pre><code>from talika import RowTable, TableError, field\n\n\nclass UserTable(RowTable):\n    email = field(\"email\", required=True)\n\n    @classmethod\n    def validate_records(cls, records, context):\n        seen = {}\n        for record in records:\n            if record.email in seen:\n                raise TableError.from_cell(\n                    \"Duplicate email\",\n                    record.source_for(\"email\"),\n                    schema=cls,\n                    field=\"email\",\n                )\n            seen[record.email] = record\n</code></pre> <p>Table validation runs after records are parsed and after local references are resolved.</p>","path":["Guides","Basic","Table Validation"],"tags":[]},{"location":"guides/basic/table-validation.html#when-to-use-it","level":2,"title":"When to use it","text":"<p>Use table validation for:</p> <ul> <li>duplicate checks</li> <li>cross-record rules</li> <li>aggregate constraints</li> <li>rules that need references to be resolved first</li> </ul> <p>Use <code>validate_record()</code> for rules local to one record.</p>","path":["Guides","Basic","Table Validation"],"tags":[]},{"location":"guides/basic/type-annotations.html","level":1,"title":"Type Annotations","text":"<p>Talika can infer parsers from common Python annotations.</p> <pre><code>from decimal import Decimal\nfrom enum import Enum\nfrom typing import Literal\n\nfrom talika import RowTable, field\n\n\nclass Status(Enum):\n    DRAFT = \"draft\"\n    PUBLISHED = \"published\"\n\n\nclass ProductTable(RowTable):\n    count: int = field(\"count\")\n    ratio: float = field(\"ratio\")\n    price: Decimal = field(\"price\")\n    active: bool = field(\"active\")\n    status: Status = field(\"status\")\n    state: Literal[\"draft\", \"published\"] = field(\"state\")\n</code></pre> <p>Supported inference:</p> <ul> <li><code>int</code></li> <li><code>float</code></li> <li><code>bool</code></li> <li><code>Decimal</code></li> <li>enums</li> <li>string <code>Literal[...]</code></li> <li>simple optionals such as <code>int | None</code></li> </ul>","path":["Guides","Basic","Type Annotations"],"tags":[]},{"location":"guides/basic/type-annotations.html#optionals","level":2,"title":"Optionals","text":"<pre><code>class UserTable(RowTable):\n    age: int | None = field(\"age\")\n</code></pre> <p>Empty strings and configured null-like tokens become <code>None</code>.</p>","path":["Guides","Basic","Type Annotations"],"tags":[]},{"location":"guides/basic/type-annotations.html#lists-are-explicit","level":2,"title":"Lists are explicit","text":"<p>Talika does not infer a cell syntax for <code>list[T]</code>.</p> <pre><code>class UserTable(RowTable):\n    tags: list[str] = field(\"tags\")\n</code></pre> <p>The value remains the raw cell text. Use <code>split()</code>, <code>each()</code>, or a custom parser when a cell should become a list.</p>","path":["Guides","Basic","Type Annotations"],"tags":[]},{"location":"guides/basic/type-annotations.html#explicit-parsers-win","level":2,"title":"Explicit parsers win","text":"<pre><code>from talika import string\n\n\nclass UserTable(RowTable):\n    code: int = field(\"code\", parser=string(upper=True))\n</code></pre> <p>The explicit parser is used instead of annotation inference.</p>","path":["Guides","Basic","Type Annotations"],"tags":[]},{"location":"learn/compiler-model.html","level":1,"title":"Compiler Model","text":"<p>Once you understand datatables and contracts, the deeper model is useful: Talika compiles a human-authored table into validated Python output.</p> <pre><code>flowchart LR\n  A[\"raw list[list[str]]\"] --&gt; B[\"TableData\"]\n  B --&gt; C[\"transform table\"]\n  C --&gt; D[\"validate shape and labels\"]\n  D --&gt; E[\"select variant\"]\n  E --&gt; F[\"parse fields\"]\n  F --&gt; G[\"build records\"]\n  G --&gt; H[\"resolve references\"]\n  H --&gt; I[\"validate records\"]\n  I --&gt; J[\"validate table\"]\n  J --&gt; K[\"build output\"]</code></pre>","path":["Learn","Compiler Model"],"tags":[]},{"location":"learn/compiler-model.html#why-this-matters","level":2,"title":"Why this matters","text":"<p>Calling Talika a parser undersells it. A parser converts a string. Talika owns the table lifecycle: shape, labels, fields, source metadata, references, validation, and output.</p>","path":["Learn","Compiler Model"],"tags":[]},{"location":"learn/compiler-model.html#extension-points","level":2,"title":"Extension points","text":"<p>You can override only the stage you need:</p> <ul> <li><code>transform_table()</code> for custom table shapes</li> <li><code>field(parser=...)</code> for cell syntax</li> <li><code>validate_record()</code> for one record</li> <li><code>validate_records()</code> for the whole table</li> <li><code>build_output()</code> for custom result objects</li> </ul> <p>Everything else keeps running around that extension.</p>","path":["Learn","Compiler Model"],"tags":[]},{"location":"learn/datatables.html","level":1,"title":"Data Tables","text":"<p>A pytest-bdd datatable is a table attached to a step. In Python, the step receives it as a raw <code>list[list[str]]</code>.</p> <p>A<pre><code>Given the users:\n  | name  | age |\n  | Alice | 30  |\n</code></pre> B<pre><code>[\n    [\"name\", \"age\"],\n    [\"Alice\", \"30\"],\n]\n</code></pre></p> <p>That raw shape is simple and flexible. It is also where table glue starts to spread across a test suite.</p>","path":["Learn","Data Tables"],"tags":[]},{"location":"learn/datatables.html#the-usual-glue","level":2,"title":"The usual glue","text":"<pre><code>headers, *rows = datatable\nusers = [dict(zip(headers, row, strict=True)) for row in rows]\n\nfor user in users:\n    user[\"age\"] = int(user[\"age\"])\n</code></pre> <p>This works until a field is renamed, a cell is empty, or one step uses <code>active=yes</code> while another uses <code>active=true</code>.</p>","path":["Learn","Data Tables"],"tags":[]},{"location":"learn/datatables.html#what-talika-changes","level":2,"title":"What Talika changes","text":"<p>Talika lets you keep the readable feature table and move the table rules into a schema:</p> <pre><code>from talika import RowTable, field\n\n\nclass UserTable(RowTable):\n    name = field(\"name\", required=True)\n    age: int = field(\"age\")\n</code></pre> <p>Now the table has a contract. Talika knows which labels are allowed, which fields are required, how each value should be parsed, and where each value came from.</p>","path":["Learn","Data Tables"],"tags":[]},{"location":"learn/datatables.html#the-important-idea","level":2,"title":"The important idea","text":"<p>Talika is not trying to make Gherkin clever. It is trying to keep the boundary between human-authored table text and Python test code explicit.</p>","path":["Learn","Data Tables"],"tags":[]},{"location":"learn/row-vs-column.html","level":1,"title":"Row vs Column Tables","text":"<p>Talika supports both common ways of writing BDD data tables.</p>","path":["Learn","Row vs Column Tables"],"tags":[]},{"location":"learn/row-vs-column.html#rowtable","level":2,"title":"RowTable","text":"<p>Use <code>RowTable</code> when the first row contains labels and each later row is one record.</p> <pre><code>| name  | role   |\n| Alice | admin  |\n| Bob   | editor |\n</code></pre> <pre><code>from talika import RowTable, field\n\n\nclass UserTable(RowTable):\n    name = field(\"name\", required=True)\n    role = field(\"role\", required=True)\n</code></pre> <p>This is the familiar spreadsheet shape.</p>","path":["Learn","Row vs Column Tables"],"tags":[]},{"location":"learn/row-vs-column.html#columntable","level":2,"title":"ColumnTable","text":"<p>Use <code>ColumnTable</code> when the first column contains labels and each later column is one record.</p> <pre><code>| IDs      | 1       | 2      |\n| Type     | Article | Poll   |\n| Headline | News    | Vote?  |\n</code></pre> <pre><code>from talika import ColumnTable, field, id_field\n\n\nclass ContentTable(ColumnTable):\n    id = id_field(\"IDs\")\n    content_type = field(\"Type\", required=True)\n    headline = field(\"Headline\", required=True)\n</code></pre> <p>Column tables are often easier to read when each item has many fields, or when non-developers naturally think of each item as a vertical card.</p>","path":["Learn","Row vs Column Tables"],"tags":[]},{"location":"learn/row-vs-column.html#ids","level":2,"title":"IDs","text":"<p><code>ColumnTable</code> requires exactly one <code>id_field</code>. The first row must be that ID row, and each item column must have a unique ID.</p> <p><code>RowTable</code> may also use an <code>id_field</code> when parsers, defaults, or diagnostics need a stable item ID.</p>","path":["Learn","Row vs Column Tables"],"tags":[]},{"location":"learn/source-aware-errors.html","level":1,"title":"Source-Aware Errors","text":"<p>Talika errors are designed for feature authors and tools.</p> <pre><code>Required field has an empty value (code=empty_required, schema=UserTable, field='name', row=2, column=1, value=''). Hint: Fill the cell, or remove required=True if an explicit empty value should be valid.\n</code></pre> <p>The message is readable, but the structured attributes are the important contract.</p>","path":["Learn","Source-Aware Errors"],"tags":[]},{"location":"learn/source-aware-errors.html#tableerror","level":2,"title":"TableError","text":"<p><code>TableError</code> represents one diagnostic. It can include:</p> <ul> <li><code>code</code></li> <li><code>schema</code></li> <li><code>field</code></li> <li><code>row</code></li> <li><code>column</code></li> <li><code>item_id</code></li> <li><code>value</code></li> <li><code>hint</code></li> </ul>","path":["Learn","Source-Aware Errors"],"tags":[]},{"location":"learn/source-aware-errors.html#tableerrors","level":2,"title":"TableErrors","text":"<p>Use collect mode to find multiple independent problems in one parse:</p> <pre><code>UserTable.parse(datatable, error_mode=\"collect\")\n</code></pre> <p>Example output:</p> <pre><code>Table contains 2 errors:\n  1. Required field has an empty value (code=empty_required, schema=UserTable, field='name', row=2, column=1, value=''). Hint: Fill the cell, or remove required=True if an explicit empty value should be valid.\n  2. Field parser failed: invalid literal for int() with base 10: 'old' (code=parser_failed, schema=UserTable, field='age', row=2, column=2, value='old'). Hint: Check the cell value or adjust the field parser for this syntax.\n</code></pre>","path":["Learn","Source-Aware Errors"],"tags":[]},{"location":"learn/source-aware-errors.html#stable-codes","level":2,"title":"Stable codes","text":"<p>Human messages may improve over time. Integrations should use stable error codes such as <code>missing_required</code>, <code>empty_required</code>, <code>parser_failed</code>, <code>reference_failed</code>, and <code>unknown_variant</code>.</p>","path":["Learn","Source-Aware Errors"],"tags":[]},{"location":"learn/table-contracts.html","level":1,"title":"Table Contracts","text":"<p>A Talika schema is a table contract written as a Python class.</p> <pre><code>from talika import RowTable, field\n\n\nclass UserTable(RowTable):\n    name = field(\"name\", required=True)\n    age: int = field(\"age\")\n</code></pre> <p>The contract says:</p> <ul> <li>the table may contain a <code>name</code> column</li> <li><code>name</code> is required</li> <li>the table may contain an <code>age</code> column</li> <li><code>age</code> should be parsed as an <code>int</code></li> </ul>","path":["Learn","Table Contracts"],"tags":[]},{"location":"learn/table-contracts.html#labels-and-attributes","level":2,"title":"Labels and attributes","text":"<p>The label is what appears in the feature table:</p> <pre><code>name = field(\"Full name\", required=True)\n</code></pre> <p>The attribute is what Python code reads:</p> <pre><code>user.name\n</code></pre> <p>This lets feature files use readable labels while test code keeps normal Python names.</p>","path":["Learn","Table Contracts"],"tags":[]},{"location":"learn/table-contracts.html#parse-apis","level":2,"title":"Parse APIs","text":"<p>Use <code>parse()</code> for normal test code:</p> <pre><code>users = UserTable.parse(datatable)\n</code></pre> <p>Use <code>parse_records()</code> when you specifically want schema record objects before output conversion:</p> <pre><code>records: list[UserTable] = UserTable.parse_records(datatable)\n</code></pre> <p>The functional APIs are thin delegates for teams that prefer schema-as-argument style:</p> <pre><code>from talika import parse_table\n\nusers = parse_table(UserTable, datatable)\n</code></pre>","path":["Learn","Table Contracts"],"tags":[]},{"location":"learn/typed-records.html","level":1,"title":"Typed Records","text":"<p>By default, <code>parse()</code> returns instances of your schema class.</p> <pre><code>users = UserTable.parse([\n    [\"name\", \"age\"],\n    [\"Alice\", \"30\"],\n])\n\nassert users[0].name == \"Alice\"\nassert users[0].age == 30\n</code></pre> <p>Those records are lightweight objects built after parsing and validation.</p>","path":["Learn","Typed Records"],"tags":[]},{"location":"learn/typed-records.html#record-helpers","level":2,"title":"Record helpers","text":"<p>Use <code>as_dict()</code> when you need declared fields as a plain dictionary:</p> <pre><code>assert users[0].as_dict() == {\"name\": \"Alice\", \"age\": 30}\n</code></pre> <p>Use <code>source_for()</code> when custom validation needs to raise an error at a specific cell:</p> <pre><code>cell = users[0].source_for(\"age\")\n</code></pre>","path":["Learn","Typed Records"],"tags":[]},{"location":"learn/typed-records.html#output-models","level":2,"title":"Output models","text":"<p>Schemas can return your own objects:</p> <pre><code>from dataclasses import dataclass\nfrom talika import RowTable, field\n\n\n@dataclass(frozen=True)\nclass User:\n    name: str\n    age: int\n\n\nclass UserTable(RowTable):\n    output_model = User\n\n    name = field(\"name\")\n    age: int = field(\"age\")\n</code></pre> <p><code>UserTable.parse(...)</code> now returns <code>list[User]</code>. <code>parse_records()</code> still returns schema records when you need source metadata or intermediate values.</p>","path":["Learn","Typed Records"],"tags":[]},{"location":"reference/index.html","level":1,"title":"Reference","text":"<p>Reference pages are for exact names, arguments, policies, and contracts.</p> <p>Start with guides when you are learning a concept. Use this section when you already know what you are looking for.</p>","path":["Reference"],"tags":[]},{"location":"reference/index.html#pages","level":2,"title":"Pages","text":"<ul> <li>Schema: table classes, parse APIs, hooks, policies</li> <li>Fields: field declarations, IDs, discriminators, references</li> <li>Parsers: parser factories and annotation inference</li> <li>Errors: <code>TableError</code>, <code>TableErrors</code>, and stable codes</li> <li>Source And Context: <code>TableData</code>, <code>TableCell</code>, contexts</li> <li>CellDSL: token, pattern, predicate, fallback, composition APIs</li> <li>Transformers: table transformers and group expansion</li> <li>CLI: <code>talika check</code>, <code>talika describe</code>, checker APIs</li> <li>Public API: top-level exports</li> </ul>","path":["Reference"],"tags":[]},{"location":"reference/cli.html","level":1,"title":"CLI","text":"<p>The CLI is installed with:</p> Install CLI<pre><code>$ pip install \"talika[cli]\"\n</code></pre>","path":["Reference","CLI"],"tags":[]},{"location":"reference/cli.html#check","level":2,"title":"check","text":"Run Static Checks<pre><code>$ talika check PATH... --schema module:SchemaClass\n</code></pre> <p>Options:</p> <ul> <li><code>--step TEXT</code></li> <li><code>--scenario NAME</code></li> <li><code>--format text|json</code></li> <li><code>--context-factory module:function</code></li> </ul> <p>Exit codes:</p> <ul> <li><code>0</code>: valid</li> <li><code>1</code>: diagnostics found</li> <li><code>2</code>: no matching tables</li> </ul>","path":["Reference","CLI"],"tags":[]},{"location":"reference/cli.html#describe","level":2,"title":"describe","text":"Describe Schema<pre><code>$ talika describe module:SchemaClass\n$ talika describe module:SchemaClass --format json\n</code></pre>","path":["Reference","CLI"],"tags":[]},{"location":"reference/cli.html#checker-apis","level":2,"title":"Checker APIs","text":"<pre><code>discover_feature_tables(path, *, step=None, scenario=None)\ncheck_feature(path, *, schema, step=None, scenario=None, context=None)\n</code></pre> <p><code>FeatureTable</code> describes one discovered table. <code>FeatureDiagnostic</code> pairs a feature-table location with a <code>TableError</code>.</p>","path":["Reference","CLI"],"tags":[]},{"location":"reference/dsl.html","level":1,"title":"CellDSL","text":"<p><code>CellDSL</code> is a parser object with project-owned dispatch rules.</p>","path":["Reference","CellDSL"],"tags":[]},{"location":"reference/dsl.html#registration","level":2,"title":"Registration","text":"<pre><code>dsl = CellDSL()\n\n@dsl.token(\"random\", fields=(\"headline\",))\ndef handler(context): ...\n\n@dsl.pattern(r\"(?P&lt;count&gt;\\d+) words\")\ndef handler(match, context): ...\n\n@dsl.when(predicate)\ndef handler(value, context): ...\n\n@dsl.fallback\ndef handler(value, context): ...\n</code></pre> <p>Field scopes use schema attribute names.</p>","path":["Reference","CellDSL"],"tags":[]},{"location":"reference/dsl.html#dispatch-order","level":2,"title":"Dispatch order","text":"<ol> <li>exact tokens</li> <li>regular-expression full matches</li> <li>predicates</li> <li>fallback</li> </ol> <p>Unmatched values pass through unchanged unless a fallback exists.</p>","path":["Reference","CellDSL"],"tags":[]},{"location":"reference/dsl.html#composition","level":2,"title":"Composition","text":"<pre><code>chain = dsl.compose(other)\nchain = compose_cell_dsls(first, second)\n</code></pre> <p><code>CellDSLChain</code> asks DSLs left-to-right and returns the first match.</p>","path":["Reference","CellDSL"],"tags":[]},{"location":"reference/errors.html","level":1,"title":"Errors","text":"<p>Talika errors are structured diagnostics.</p>","path":["Reference","Errors"],"tags":[]},{"location":"reference/errors.html#tableerror","level":2,"title":"TableError","text":"<p>Attributes:</p> <ul> <li><code>message</code></li> <li><code>schema</code></li> <li><code>field</code></li> <li><code>row</code></li> <li><code>column</code></li> <li><code>item_id</code></li> <li><code>value</code></li> <li><code>code</code></li> <li><code>hint</code></li> </ul> <p>Use <code>TableError.from_cell(message, cell, ...)</code> to create a diagnostic at a specific source cell.</p>","path":["Reference","Errors"],"tags":[]},{"location":"reference/errors.html#tableerrors","level":2,"title":"TableErrors","text":"<p><code>TableErrors</code> aggregates multiple <code>TableError</code> objects in collect mode.</p> <pre><code>try:\n    UserTable.parse(datatable, error_mode=\"collect\")\nexcept TableErrors as exc:\n    for error in exc:\n        print(error.code, error.row, error.column)\n</code></pre>","path":["Reference","Errors"],"tags":[]},{"location":"reference/errors.html#error-codes","level":2,"title":"Error codes","text":"<p>Stable <code>TableErrorCode</code> values:</p> <ul> <li><code>table_error</code></li> <li><code>schema_definition</code></li> <li><code>invalid_context</code></li> <li><code>table_empty</code></li> <li><code>header_empty</code></li> <li><code>ragged_row</code></li> <li><code>duplicate_label</code></li> <li><code>unknown_field</code></li> <li><code>missing_required</code></li> <li><code>empty_required</code></li> <li><code>empty_optional</code></li> <li><code>default_factory_failed</code></li> <li><code>parser_failed</code></li> <li><code>transform_failed</code></li> <li><code>invalid_transform</code></li> <li><code>unknown_variant</code></li> <li><code>inapplicable_field</code></li> <li><code>duplicate_id</code></li> <li><code>reference_failed</code></li> <li><code>record_validation_failed</code></li> <li><code>table_validation_failed</code></li> <li><code>output_failed</code></li> </ul> <p>Tools should use codes instead of scraping human-readable error text.</p>","path":["Reference","Errors"],"tags":[]},{"location":"reference/fields.html","level":1,"title":"Fields","text":"<p>Field declarations are descriptors collected by schema classes.</p>","path":["Reference","Fields"],"tags":[]},{"location":"reference/fields.html#field","level":2,"title":"field","text":"<pre><code>field(\n    label,\n    *,\n    required=False,\n    default=MISSING,\n    default_factory=MISSING,\n    parser=None,\n    aliases=(),\n    empty=\"raw\",\n)\n</code></pre> <p><code>empty</code> accepts:</p> <ul> <li><code>\"raw\"</code>: keep <code>\"\"</code></li> <li><code>\"parse\"</code>: send <code>\"\"</code> to the parser</li> <li><code>\"none\"</code>: return <code>None</code></li> <li><code>\"error\"</code>: reject <code>\"\"</code></li> </ul> <p>Required fields cannot declare defaults.</p>","path":["Reference","Fields"],"tags":[]},{"location":"reference/fields.html#id_field","level":2,"title":"id_field","text":"<pre><code>id_field(label, *, parser=None, aliases=())\n</code></pre> <p><code>ColumnTable</code> requires exactly one <code>id_field</code>. <code>RowTable</code> may use one when parser context, default factories, or diagnostics need an <code>item_id</code>.</p>","path":["Reference","Fields"],"tags":[]},{"location":"reference/fields.html#discriminator_field","level":2,"title":"discriminator_field","text":"<pre><code>discriminator_field(label, *, parser=None, aliases=())\n</code></pre> <p>Declares the field used to select explicit variants registered with <code>@Table.variant(value)</code>.</p>","path":["Reference","Fields"],"tags":[]},{"location":"reference/fields.html#discriminator","level":2,"title":"discriminator","text":"<pre><code>discriminator(label, *, variants, parser=None, aliases=())\n</code></pre> <p><code>variants</code> maps parsed discriminator values to <code>TableFields</code> subclasses. Talika composes those components with the base table schema.</p>","path":["Reference","Fields"],"tags":[]},{"location":"reference/fields.html#reference","level":2,"title":"reference","text":"<pre><code>reference(\n    label,\n    *,\n    target=\"id\",\n    many=False,\n    separator=\",\",\n    required=False,\n    default=MISSING,\n    aliases=(),\n)\n</code></pre> <p>References resolve to records in the same parsed table.</p>","path":["Reference","Fields"],"tags":[]},{"location":"reference/parsers.html","level":1,"title":"Parsers","text":"<p>Parsers are callables with this shape:</p> <pre><code>def parser(value, context):\n    ...\n</code></pre> <p><code>context</code> is a <code>CellContext</code>.</p>","path":["Reference","Parsers"],"tags":[]},{"location":"reference/parsers.html#factories","level":2,"title":"Factories","text":"<pre><code>string(strip=False, lower=False, upper=False)\ninteger(base=10)\nfloating()\ndecimal()\nboolean(true_values=(\"true\", \"yes\", \"1\", \"on\"), false_values=(\"false\", \"no\", \"0\", \"off\"), case_sensitive=False)\nchoice(*values, case_sensitive=True)\nsplit(separator=\",\", strip_items=True, keep_empty=False)\nmap_value(values, case_sensitive=True)\ncompose(*parsers)\neach(parser)\noptional(parser, none_values=(\"none\", \"null\"), case_sensitive=False)\n</code></pre> <p>Configuration errors raise <code>ValueError</code> when the parser is built.</p> <p>Runtime parser failures are wrapped as <code>TableError</code> with source-cell details.</p>","path":["Reference","Parsers"],"tags":[]},{"location":"reference/parsers.html#annotation-inference","level":2,"title":"Annotation inference","text":"<p>Talika infers parsers for:</p> <ul> <li><code>int</code></li> <li><code>float</code></li> <li><code>bool</code></li> <li><code>Decimal</code></li> <li>enums</li> <li>string <code>Literal[...]</code></li> <li>simple optionals such as <code>int | None</code></li> </ul> <p>Explicit <code>field(parser=...)</code> always wins.</p> <p>Unsupported annotations leave the value unchanged. <code>list[T]</code> does not infer a cell syntax.</p>","path":["Reference","Parsers"],"tags":[]},{"location":"reference/public-api.html","level":1,"title":"Public API","text":"<p>Import normal project APIs from <code>talika</code>.</p>","path":["Reference","Public API"],"tags":[]},{"location":"reference/public-api.html#errors","level":2,"title":"Errors","text":"<ul> <li><code>TableError</code></li> <li><code>TableErrorCode</code></li> <li><code>TableErrors</code></li> <li><code>SchemaDefinitionError</code></li> </ul>","path":["Reference","Public API"],"tags":[]},{"location":"reference/public-api.html#schemas-and-fields","level":2,"title":"Schemas and fields","text":"<ul> <li><code>RowTable</code></li> <li><code>ColumnTable</code></li> <li><code>TableFields</code></li> <li><code>Field</code></li> <li><code>ReferenceSpec</code></li> <li><code>field</code></li> <li><code>id_field</code></li> <li><code>discriminator</code></li> <li><code>discriminator_field</code></li> <li><code>reference</code></li> </ul>","path":["Reference","Public API"],"tags":[]},{"location":"reference/public-api.html#parsers","level":2,"title":"Parsers","text":"<ul> <li><code>string</code></li> <li><code>integer</code></li> <li><code>floating</code></li> <li><code>decimal</code></li> <li><code>boolean</code></li> <li><code>choice</code></li> <li><code>split</code></li> <li><code>map_value</code></li> <li><code>compose</code></li> <li><code>each</code></li> <li><code>optional</code></li> </ul>","path":["Reference","Public API"],"tags":[]},{"location":"reference/public-api.html#dsl","level":2,"title":"DSL","text":"<ul> <li><code>CellDSL</code></li> <li><code>CellDSLChain</code></li> <li><code>compose_cell_dsls</code></li> </ul>","path":["Reference","Public API"],"tags":[]},{"location":"reference/public-api.html#source-and-context","level":2,"title":"Source and context","text":"<ul> <li><code>TableData</code></li> <li><code>TableCell</code></li> <li><code>RecordSource</code></li> <li><code>ParseContext</code></li> <li><code>CellContext</code></li> <li><code>DefaultContext</code></li> </ul>","path":["Reference","Public API"],"tags":[]},{"location":"reference/public-api.html#transformations","level":2,"title":"Transformations","text":"<ul> <li><code>TableTransformer</code></li> <li><code>TransformerPipeline</code></li> <li><code>compose_transformers</code></li> <li><code>ColumnGroupExpander</code></li> <li><code>NumericRange</code></li> <li><code>AlphabeticRange</code></li> <li><code>PrefixRepeat</code></li> <li><code>SuffixRepeat</code></li> <li><code>RangeRule</code></li> <li><code>RepeatRule</code></li> </ul>","path":["Reference","Public API"],"tags":[]},{"location":"reference/public-api.html#tooling-and-contracts","level":2,"title":"Tooling and contracts","text":"<ul> <li><code>FieldContract</code></li> <li><code>TableContract</code></li> <li><code>VariantContract</code></li> <li><code>FeatureTable</code></li> <li><code>FeatureDiagnostic</code></li> <li><code>discover_feature_tables</code></li> <li><code>check_feature</code></li> <li><code>parse_table</code></li> <li><code>parse_table_records</code></li> <li><code>__version__</code></li> </ul>","path":["Reference","Public API"],"tags":[]},{"location":"reference/schema.html","level":1,"title":"Schema","text":"<p>Schema classes declare table contracts.</p>","path":["Reference","Schema"],"tags":[]},{"location":"reference/schema.html#classes","level":2,"title":"Classes","text":"<ul> <li><code>RowTable</code>: first row contains labels; later rows are records</li> <li><code>ColumnTable</code>: first column contains labels; later columns are records</li> <li><code>TableFields</code>: reusable field group for variants or mixins</li> </ul>","path":["Reference","Schema"],"tags":[]},{"location":"reference/schema.html#parse-methods","level":2,"title":"Parse methods","text":"<pre><code>Schema.parse(datatable, *, context=None, error_mode=\"first\")\nSchema.parse_records(datatable, *, context=None, error_mode=\"first\")\n</code></pre> <p><code>parse()</code> returns public output objects. <code>parse_records()</code> returns schema record instances and skips output conversion.</p> <p><code>datatable</code> may be raw <code>list[list[str]]</code> or <code>TableData</code>.</p> <p><code>error_mode</code> accepts:</p> <ul> <li><code>\"first\"</code></li> <li><code>\"collect\"</code></li> </ul>","path":["Reference","Schema"],"tags":[]},{"location":"reference/schema.html#hooks","level":2,"title":"Hooks","text":"<pre><code>def validate_record(self, context): ...\n\n@classmethod\ndef validate_records(cls, records, context): ...\n\n@classmethod\ndef build_output(cls, record, context): ...\n\n@classmethod\ndef transform_table(cls, table, context): ...\n</code></pre>","path":["Reference","Schema"],"tags":[]},{"location":"reference/schema.html#class-attributes","level":2,"title":"Class attributes","text":"<pre><code>table_transformer = None\noutput_model = None\nunknown_fields = \"forbid\"\ninapplicable_fields = \"forbid\"\n</code></pre> <p><code>unknown_fields</code> currently supports only <code>\"forbid\"</code>.</p> <p><code>inapplicable_fields</code> supports <code>\"forbid\"</code> and <code>\"preserve\"</code>.</p>","path":["Reference","Schema"],"tags":[]},{"location":"reference/schema.html#variants","level":2,"title":"Variants","text":"<pre><code>@BaseTable.variant(value)\nclass ConcreteVariant(BaseTable):\n    ...\n</code></pre> <p>Use <code>variant_for(value)</code> to retrieve a registered variant class.</p> <p>Use <code>describe()</code> to return a <code>TableContract</code>.</p>","path":["Reference","Schema"],"tags":[]},{"location":"reference/source-context.html","level":1,"title":"Source And Context","text":"","path":["Reference","Source And Context"],"tags":[]},{"location":"reference/source-context.html#tablecell","level":2,"title":"TableCell","text":"<pre><code>TableCell(value, source_row, source_column, source_value)\n</code></pre> <p>Use <code>TableCell.from_value(value, row=..., column=...)</code> for untransformed cells. Use <code>cell.with_value(new_value)</code> inside transformations to preserve source coordinates.</p>","path":["Reference","Source And Context"],"tags":[]},{"location":"reference/source-context.html#tabledata","level":2,"title":"TableData","text":"<pre><code>TableData.from_rows(rows)\nTableData.from_cells(rows)\nTableData.ensure(table)\ntable.cell(row, column)\ntable.to_rows()\n</code></pre> <p><code>cell(row, column)</code> uses one-based indexes.</p>","path":["Reference","Source And Context"],"tags":[]},{"location":"reference/source-context.html#recordsource","level":2,"title":"RecordSource","text":"<p>Parsed records expose:</p> <pre><code>record.table_source\nrecord.source_for(\"field_name\")\n</code></pre> <p>Missing optional fields have no source cell.</p>","path":["Reference","Source And Context"],"tags":[]},{"location":"reference/source-context.html#parsecontext","level":2,"title":"ParseContext","text":"<pre><code>ParseContext.from_value(value)\n</code></pre> <p>Normalizes <code>None</code>, mappings, and existing <code>ParseContext</code> objects. User mappings are copied into read-only <code>user_data</code>.</p>","path":["Reference","Source And Context"],"tags":[]},{"location":"reference/source-context.html#cellcontext","level":2,"title":"CellContext","text":"<p>Passed to field parsers. Contains schema, field name, label, source row, source column, item ID, source value, and user data.</p>","path":["Reference","Source And Context"],"tags":[]},{"location":"reference/source-context.html#defaultcontext","level":2,"title":"DefaultContext","text":"<p>Passed to default factories. Contains schema, field name, label, item ID, and user data.</p>","path":["Reference","Source And Context"],"tags":[]},{"location":"reference/transformers.html","level":1,"title":"Transformers","text":"<p>Transformers convert <code>TableData</code> before schema parsing.</p>","path":["Reference","Transformers"],"tags":[]},{"location":"reference/transformers.html#protocol","level":2,"title":"Protocol","text":"<pre><code>def transform(self, table, context, *, schema=None):\n    ...\n</code></pre> <p>Return <code>TableData</code>. Use <code>TableCell.with_value()</code> when derived values should keep original source coordinates.</p>","path":["Reference","Transformers"],"tags":[]},{"location":"reference/transformers.html#pipeline","level":2,"title":"Pipeline","text":"<pre><code>TransformerPipeline([first, second])\ncompose_transformers(first, second)\n</code></pre> <p>Transformers run left-to-right.</p>","path":["Reference","Transformers"],"tags":[]},{"location":"reference/transformers.html#column-group-expansion","level":2,"title":"Column group expansion","text":"<pre><code>ColumnGroupExpander(\n    key_row,\n    range_rule,\n    repeat_rule,\n)\n</code></pre> <p>Built-in range rules:</p> <ul> <li><code>NumericRange(separator=\"..\")</code></li> <li><code>AlphabeticRange(separator=\"-\")</code></li> </ul> <p>Built-in repeat rules:</p> <ul> <li><code>PrefixRepeat(separator=\":\")</code></li> <li><code>SuffixRepeat(separator=\" x\")</code></li> </ul> <p>Custom <code>RangeRule</code> and <code>RepeatRule</code> objects must return <code>TableCell</code> values.</p>","path":["Reference","Transformers"],"tags":[]},{"location":"start/install.html","level":1,"title":"Installation","text":"<p>Talika supports Python 3.10 and newer.</p> <p>The normal install is intentionally small. Core Talika has no runtime dependencies, so you can use schemas, parsers, validation, Custom DSL, table transforms, and source-aware errors without pulling in a larger test stack.</p> <p>We recommend <code>uv</code> for new projects. It is fast, creates virtual environments easily, and keeps dependency changes explicit in your project. If your project already uses <code>pip</code> and <code>venv</code>, Talika works there too.</p>","path":["Getting Started","Installation"],"tags":[]},{"location":"start/install.html#virtual-environment","level":2,"title":"Virtual Environment","text":"<p>Install Talika inside a virtual environment so your test dependencies stay separate from your system Python.</p> <p>Use the <code>uv</code> tab if you are starting fresh. Use the <code>pip</code> tab if that matches the rest of your project.</p>  uv   pip Create env with uv<pre><code>$ uv venv\n\n# Activate the environment:\n\nFor windows\n&gt;&gt; .venv\\Scripts\\activate.ps1\n\nFor Mac|Linux\n$ source .venv/bin/activate\n</code></pre> Create env with venv<pre><code>$ python -m venv .venv\n\n# Activate the environment:\n\nFor windows\n&gt;&gt; .venv\\Scripts\\activate.ps1\n\nFor Mac|Linux\n$ source .venv/bin/activate\n</code></pre>","path":["Getting Started","Installation"],"tags":[]},{"location":"start/install.html#install-talika","level":2,"title":"Install Talika","text":"<p>For most users, start with the core package.</p> <p>You can add optional extras later when you need CLI checks or Pydantic output.</p>  uv   pip Install Talika with uv<pre><code>$ uv add talika\n</code></pre> Install Talika with pip<pre><code>$ pip install talika\n</code></pre>","path":["Getting Started","Installation"],"tags":[]},{"location":"start/install.html#optional-extras","level":2,"title":"Optional extras","text":"<p>Install only the integrations you use. The extras are separate so the core library can stay dependency-free.</p>  uv   pip Install Optional extras with uv<pre><code>$ uv add talika[cli]\n\nUse this when you want to check `.feature` files without running the full test\nsuite. It installs the official Gherkin parser used by `talika check` and\nfeature-file discovery.\n\nThis is useful for CI, pre-commit checks, and editor tooling.\n\n\n$ uv add talika[pydantic]\n\nUse this when your schemas should return Pydantic v2 models through\noutput_model.\n\nTalika still owns the table boundary: labels, cell parsing, source locations,\nand table validation. Pydantic owns the final model validation.\n\n\n$ uv add talika[test]      \n\nUse this when you want the dependencies needed for the package tests and\nrunnable examples. It includes pytest, pytest-bdd, the CLI dependencies, and\nPydantic.\n\nMost application projects do not need this extra unless they are contributing\nto Talika or running the example suite locally.\n</code></pre> Install Optional extras with pip<pre><code>talika check and feature-file discovery\n\nUse this when you want to check `.feature` files without running the full test\nsuite. It installs the official Gherkin parser used by `talika check` and\nfeature-file discovery.\n\n\nThis is useful for CI, pre-commit checks, and editor tooling.\n\n\n$ pip install talika[cli]\n\nPydantic v2 output models\n$ pip install talika[pydantic]\n\nproject test dependencies\n$ pip install talika[test] \n</code></pre>","path":["Getting Started","Installation"],"tags":[]},{"location":"start/install.html#verify-the-install","level":2,"title":"Verify the install","text":"<p>After installation, import Talika from Python:</p> Check Python Import<pre><code>$ python -c \"import talika; print(talika.__version__)\"\n</code></pre> <p>If you installed the CLI extra, the command line interface is available as:</p> Check CLI Entrypoints<pre><code>$ talika --help\n$ python -m talika --help\n</code></pre>","path":["Getting Started","Installation"],"tags":[]},{"location":"start/install.html#what-gets-installed","level":2,"title":"What gets installed","text":"<p>Core parsing imports only the Python standard library. The official Gherkin parser is loaded lazily by the CLI and checker APIs, so ordinary schema parsing does not depend on it.</p> <p>Use the CLI extra when you want to validate <code>.feature</code> files without running pytest:</p> Install CLI Extra<pre><code>$ uv add talika[cli]\n</code></pre> <p>Use the Pydantic extra only when your parsed records should become Pydantic models:</p> Install Pydantic Extra<pre><code>$ uv add talika[pydantic]\n</code></pre> <p>If you are unsure, install only <code>talika</code> first. You can add an extra later without changing your schemas.</p>","path":["Getting Started","Installation"],"tags":[]},{"location":"start/quickstart.html","level":1,"title":"Quickstart","text":"<p>This page starts with a plain pytest-bdd datatable shape and adds one Talika contract.</p>","path":["Getting Started","Quickstart"],"tags":[]},{"location":"start/quickstart.html#1-start-with-the-table","level":2,"title":"1. Start with the table","text":"<p>pytest-bdd supplies a datatable as nested strings:</p> <pre><code>datatable = [\n    [\"name\", \"age\"],\n    [\"Alice\", \"30\"],\n]\n</code></pre>","path":["Getting Started","Quickstart"],"tags":[]},{"location":"start/quickstart.html#2-declare-a-row-table","level":2,"title":"2. Declare a row table","text":"<pre><code>from talika import RowTable, field\n\n\nclass UserTable(RowTable):\n    name = field(\"name\", required=True)\n    age: int = field(\"age\")\n</code></pre> <p>The field labels are the words in the feature table. The Python attribute names are what your test code uses.</p>","path":["Getting Started","Quickstart"],"tags":[]},{"location":"start/quickstart.html#3-parse-typed-records","level":2,"title":"3. Parse typed records","text":"<pre><code>users = UserTable.parse(datatable)\n\nassert users[0].name == \"Alice\"\nassert users[0].age == 30\n</code></pre> <p>The <code>age: int</code> annotation gives Talika an integer parser. Explicit parsers are available when you want project-specific syntax.</p>","path":["Getting Started","Quickstart"],"tags":[]},{"location":"start/quickstart.html#4-see-the-error","level":2,"title":"4. See the error","text":"<pre><code>UserTable.parse([\n    [\"name\", \"age\"],\n    [\"\", \"old\"],\n])\n</code></pre> <p>The first failure is source-aware:</p> <pre><code>Required field has an empty value \n\n(code=empty_required, schema=UserTable, field='name', row=2, column=1, value=''). \n\nHint: Fill the cell, or remove required=True if an explicit empty value should be valid.\n</code></pre> <p>Use collect mode when authors should see every independent problem in one pass:</p> <pre><code>UserTable.parse(datatable, error_mode=\"collect\")\n</code></pre>","path":["Getting Started","Quickstart"],"tags":[]},{"location":"start/quickstart.html#5-use-it-in-a-pytest-bdd-step","level":2,"title":"5. Use it in a pytest-bdd step","text":"<pre><code>from pytest_bdd import given\n\n\n@given(\"the users:\", target_fixture=\"users\")\ndef users(datatable):\n    return UserTable.parse(datatable)\n</code></pre> <p>Talika does not replace pytest-bdd. It handles the table boundary and returns objects your tests can use.</p>","path":["Getting Started","Quickstart"],"tags":[]},{"location":"start/why.html","level":1,"title":"Why Talika?","text":"<p>We built Talika because we needed BDD data tables to stay simple for authors and still be reliable for test code.</p> <p>The core package has zero runtime dependencies. You can start with plain Python schemas, parse ordinary <code>list[list[str]]</code> tables, and add only the optional extras you need for CLI checks or Pydantic output. At the same time, Talika gives you the pieces to build your own table language: schema validation, typed cell parsers, custom cell DSL rules, source-aware errors, variants, references, table transforms, and output models.</p> <p>In <code>pytest-bdd</code>, a Gherkin data table reaches the step function as a raw <code>list[list[str]]</code>. That is simple and flexible, but it means every project has to decide what the table means:</p> <ul> <li>which row or column contains labels</li> <li>which fields are required</li> <li>how strings become <code>int</code>, <code>bool</code>, <code>Decimal</code>, enums, or lists</li> <li>what an empty cell means</li> <li>whether old labels are still accepted</li> <li>what custom words like <code>random</code>, <code>today</code>, or <code>3 Articles</code> mean</li> <li>where to point the user when a value is wrong</li> </ul> <p>For one small table, hand-written parsing is fine. In a real test suite, that glue starts to spread across step definitions.</p> A typical first parser<pre><code>headers, *rows = datatable\n\nusers = []\nfor row in rows:\n    raw = dict(zip(headers, row, strict=True))\n    users.append({\n        \"name\": raw[\"name\"],\n        \"age\": int(raw[\"age\"]),\n        \"active\": raw[\"active\"].lower() in {\"true\", \"yes\", \"1\"},\n    })\n</code></pre> <p>The problem is not the code above. The problem is having many versions of it: different boolean words, different defaults, different error messages, and tracebacks that point to Python code instead of the cell in the <code>.feature</code> file.</p>","path":["Getting Started","Why Talika?"],"tags":[]},{"location":"start/why.html#the-missing-layer","level":2,"title":"The missing layer","text":"<p>Cucumber-JVM has a <code>DataTableType</code> layer for converting Gherkin tables into objects. Python teams using <code>pytest-bdd</code> get the raw table and must build that layer themselves.</p> <p>That missing layer is where most of the important table decisions actually live. It is not just \"turn rows into dictionaries\". A useful table layer should know the expected labels, choose the right orientation, parse cell values, apply defaults, reject unknown or misspelled fields, support project vocabulary, and return errors that point back to the feature file.</p> <p>Without that layer, these rules usually end up scattered across step definitions:</p> <ul> <li>one step accepts <code>yes</code> and <code>no</code>, another accepts <code>true</code> and <code>false</code></li> <li>one table treats an empty cell as <code>None</code>, another keeps it as <code>\"\"</code></li> <li>one parser supports old labels, another breaks when wording changes</li> <li>one failure says which cell is wrong, another only raises <code>ValueError</code></li> </ul> <p>Talika fills this gap for Python. It sits between the BDD framework and your test setup code</p> <p>The idea is deliberately small: keep the feature table readable, move table rules into one reusable contract, and let the rest of the test work with normal Python objects.</p> Gherkin<pre><code>    Given The following users are present\n      | name  | age | roles               | active |\n      | Akash | 27  | Developer,Manager   | Yes    |\n      | Badal | 25  | Tester,Scrum Master | No     |\n</code></pre> A Talika table contract<pre><code>from talika import RowTable, boolean, field, split\n\n\nclass UserTable(RowTable):# (1)!\n    name = field(\"name\", required=True) # (2)!\n    age: int = field(\"age\") # (3)!\n    roles = field(\"roles\", parser=split(\",\")) # (4)!\n    active = field(\"active\", parser=boolean(), default=True) # (5)!\n\n\nusers = UserTable.parse(datatable) # (6)!\n</code></pre> <ol> <li> <p>Defines a new row-oriented table schema. When parsed, each data row in the BDD table will be validated and converted into a <code>UserTable</code> record instance.</p> </li> <li> <p><code>required=True</code></p> <p>This column must be present in the table and its cells cannot be empty.</p> </li> <li> <p>Because it is annotated with : <code>int</code>, talika's metaclass automatically infers and assigns an integer parser to this field. You don't need to explicitly pass <code>parser=integer()</code>.</p> </li> <li> <p><code>parser=split(\",\")</code></p> <p>Takes the raw string from the table cell and splits it by commas into a list <code>[str]</code>. By default, it automatically strips whitespace around each item and ignores empty segments.</p> </li> <li> <p><code>parser=boolean()</code></p> <p>Strictly converts tokens like \"true\", \"false\", \"yes\", \"no\" into a bool. Unknown values will raise a validation error   instead of relying on Python's truthiness.</p> <p><code>default=True</code></p> <p>If the \"active\" column is entirely missing from the BDD table, the value for all parsed records will default to True.</p> </li> <li> <p>Takes raw rows (or source-aware <code>TableData</code>) from <code>datatable</code>, matches the headers to your declared fields,</p> <p>runs the specified parsers (like <code>split</code> and <code>boolean</code>), and validates everything.</p> <p>Returns a list of validated <code>UserTable</code> record objects.</p> </li> </ol> User<pre><code>&gt;&gt; users\n[\n    UserTable(name='Akash', age=27, roles=['Developer', 'Manager'], active=True), \n    UserTable(name='Badal', age=25, roles=['Tester', 'Scrum Master'], active=False)\n]\n\n\n&gt;&gt; users[0]\nUserTable(name='Akash', age=27, roles=['Developer', 'Manager'], active=True)\n\n\n&gt;&gt; users[0].name\n'Akash'\n\n\n&gt;&gt; users[0].age\n27\n\n\n&gt;&gt; type(users[0].age)\n&lt;class 'int'&gt;\n\n\n&gt;&gt; users[0].roles\n['Developer', 'Manager']\n</code></pre> <p>Now the table rules live in one reusable contract. The step receives typed records, and bad data fails with the field, row, column, item ID when present, original value, stable error code, and a human-readable hint.</p>","path":["Getting Started","Why Talika?"],"tags":[]},{"location":"start/why.html#what-talika-gives-you","level":2,"title":"What Talika gives you","text":"<p>Talika is useful because the pieces work together:</p> <ul> <li><code>RowTable</code> and <code>ColumnTable</code> for the two common table shapes</li> <li><code>field()</code>, <code>id_field()</code>, aliases, defaults, and empty-cell policies</li> <li>parser factories for booleans, numbers, choices, lists, and composition</li> <li><code>CellDSL</code> for project-owned cell vocabulary</li> <li>variants when one table contains different record types</li> <li>references between records in the same scenario</li> <li>source-aware diagnostics with stable error codes</li> <li>optional static checks for <code>.feature</code> files</li> <li>optional output as dataclasses, Pydantic models, or custom objects</li> </ul> <p>That source-aware part matters. A useful failure should tell the author where to look:</p> <pre><code>Field parser failed: invalid literal for int() with base 10: 'old'\n(code=parser_failed, schema=UserTable, field='age', row=2, column=2, value='old').\nHint: Check the cell value or adjust the field parser for this syntax.\n</code></pre> <p>The error points to the authored table, not only to the parser function.</p>","path":["Getting Started","Why Talika?"],"tags":[]},{"location":"start/why.html#custom-table-language","level":2,"title":"Custom table language","text":"<p>Talika does not force one DSL on every team. It gives you safe hooks to create the vocabulary your feature files need.</p> <p>For example, a compact content table might let authors write <code>1-3</code> for three IDs and <code>3 Articles</code> to repeat one value across those IDs:</p> Compact table syntax<pre><code>from talika import ColumnGroupExpander, NumericRange, PrefixRepeat\n\n\ntable_transformer = ColumnGroupExpander(\n    key_row=\"IDs\",\n    range_rule=NumericRange(\"-\"),\n    repeat_rule=PrefixRepeat(\" \"),\n)\n</code></pre> Author-friendly table<pre><code>Given the following Content is created\n| IDs  | 1-3        |\n| Type | 3 Articles |\n</code></pre> <p>Talika can expand that into three logical records while preserving the original source cell. If <code>3 Articles</code> is wrong, the diagnostic can still point to that exact cell.</p> <p>For cell-level vocabulary, use <code>CellDSL</code>:</p> Project-owned cell DSL<pre><code>from talika import CellDSL\n\n\ncells = CellDSL()\n\n\n@cells.token(\"random\", fields=(\"headline\",))\ndef random_headline(context):\n    return context.user_data[\"faker\"].headline()\n\n\n@cells.pattern(r\"(?P&lt;count&gt;\\d+) words\", fields=(\"body\",))\ndef generated_words(match, context):\n    return context.user_data[\"faker\"].words(int(match[\"count\"]))\n</code></pre> <p>Now your tables can use simple words like <code>random</code> or <code>20 words</code>, and your project decides exactly what those words mean.</p>","path":["Getting Started","Why Talika?"],"tags":[]},{"location":"start/why.html#how-talika-compares","level":2,"title":"How Talika compares","text":"Tool What it is good at What Talika adds <code>pytest-bdd</code> datatables Passing Gherkin table text into Python steps A schema contract, typed records, validation, source-aware errors, and static checking Cucumber-JVM <code>DataTableType</code> Converting Cucumber data tables in the JVM ecosystem A Python table-conversion layer designed around <code>pytest-bdd</code>, source metadata, row and column tables, variants, and table transforms Pydantic Validating Python data models after the data already has shape The earlier step: reading a two-dimensional Gherkin table, matching labels, parsing cells, preserving source coordinates, then optionally building Pydantic models <code>factory_boy</code> Creating test objects and ORM fixtures from Python factories Parsing human-authored feature tables before fixture creation Scenario Outlines Running the same scenario with different example values Structured multi-record data inside one scenario Hand-written dict parsing Quick one-off conversion Reusable parsing rules, consistent validation, better diagnostics, and less drift across steps <p>These tools can work together. Talika is not trying to replace them.</p> Talika with a factory<pre><code>records = UserTable.parse_records(datatable)\n\nusers = [\n    UserFactory(**record.as_dict())\n    for record in records\n]\n</code></pre> Talika with Pydantic output<pre><code>from pydantic import BaseModel\nfrom talika import RowTable, field\n\n\nclass User(BaseModel):\n    name: str\n    age: int\n\n\nclass UserTable(RowTable):\n    output_model = User\n\n    name = field(\"name\", required=True)\n    age: int = field(\"age\")\n\n\nusers: list[User] = UserTable.parse(datatable)\n</code></pre> <p>Pydantic validates the model. <code>factory_boy</code> can build the fixture. Talika owns the table boundary: labels, cells, source locations, table shape, and table authoring rules.</p>","path":["Getting Started","Why Talika?"],"tags":[]},{"location":"start/why.html#where-talika-shines","level":2,"title":"Where Talika shines","text":"<p>Talika is most useful when table data is part of the product language of your tests:</p> <ul> <li>QA, product, or developers edit <code>.feature</code> files directly.</li> <li>You want one reusable contract instead of repeated parsing glue.</li> <li>You need strict schema validation without giving up readable tables.</li> <li>You want a zero-dependency core and optional integrations only when needed.</li> <li>You need custom cell syntax such as <code>random</code>, <code>20 words</code>, <code>today</code>, <code>1-3</code>, or   <code>3 Articles</code>.</li> <li>You want failures to point to the original <code>.feature</code> cell, even after table   transforms.</li> <li>You have column-oriented tables, variants, local references, or table-level   validation.</li> <li>CI or editor tooling should check feature tables before the scenario runs.</li> </ul>","path":["Getting Started","Why Talika?"],"tags":[]},{"location":"start/why.html#what-talika-is-not","level":2,"title":"What Talika is not","text":"<p>Talika is intentionally narrow.</p> <p>It is not a test runner, not a replacement for <code>pytest-bdd</code>, not a fixture factory, not a business workflow engine, and not a general object validation library. It also does not force one universal Gherkin table DSL.</p> <p>Talika owns the mechanics of turning authored tables into useful Python objects. Your project owns the meaning of the table. That is the point: simple tables for humans, strong contracts for code, and enough extension points to grow with your test suite.</p>","path":["Getting Started","Why Talika?"],"tags":[]}]}